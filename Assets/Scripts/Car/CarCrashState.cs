using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum CarCrashStateEnum
{
    Spinning,
    Stop,
    StartAccelation
}
public class CarCrashState : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
